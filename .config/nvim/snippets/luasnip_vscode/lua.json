{
  "in": {
    "prefix": "in",
    "description": "init",
    "body": [
      "local status, $1 = pcall(require, '$2')",
      "if (not status) then return end",
      "",
      "$0"
    ]
  },
  "pri": {
    "prefix": "pri",
    "description": "print inspect",
    "body": ["print(vim.inspect($0))"]
  }
}
